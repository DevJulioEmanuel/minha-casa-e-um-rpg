spring:
  application:
    name: RepublicaQuestAPI
  datasource:
    url: jdbc:postgresql://localhost:5433/repquest
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
api:
  security:
    token:
      secret: ${JWT_SECRET:my-secret-key}